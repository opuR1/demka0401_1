<Page x:Class="demka1.Pages.OrderList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:demka1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderList" Background="{StaticResource MainBackground}">
    <Page.Resources>
        <Style x:Key="ProductItemStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource MainBackground}"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Add" Margin="10" x:Name="btnAdd" Height="25" Width="50" Background="{StaticResource AccentActionBackground}" FontSize="14" FontWeight="Bold" Click="btnAdd_Click"/>
        </StackPanel>
        <ListBox Grid.Row="1" x:Name="lbOrders" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Hidden" MouseDoubleClick="lbOrders_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10">
                            <TextBlock Text="{Binding Partner.PartnerFullName}" FontSize="14" Margin="5"/>
                            <TextBlock Text="{Binding Partner.PartnerAdress}" FontSize="14" Margin="5"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Тел. +7 " FontSize="14" Margin="5,5,0,5"/>
                                <TextBlock Text="{Binding Partner.Phone}" FontSize="14" Margin="0,5,0,5"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Рейтинг: " FontSize="14" Margin="5"/>
                                <TextBlock Text="{Binding Partner.Rating}" FontSize="14" Margin="5"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
                            <TextBlock Text="Стоимость" FontSize="14" Margin="5"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:N2}}" FontSize="14" Margin="5"/>
                                <TextBlock Text="руб." FontSize="14" Margin="5"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
